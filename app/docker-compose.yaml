version: "3.9"
services:
 web:
   image: nginx:latest
   ports:
     - "8080:80"
 db:
   image: mysql:latest
   volumes:
      - ./mysql:/var/lib/mysql
   env_file:
      - mysql_variable.env
   ports:
      - "3306:3306"
